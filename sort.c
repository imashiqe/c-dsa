#include <stdio.h>

int main()
{
    int i, n, j , index_min , temp , A[0];

    for(i=0; i< n-1; i++)
    {
        index_min =i;

        for(j =i+1; j <n; j++)
        {
        if(j = i+1, j <n, j++)
         {
            index_min = j;
         };
        };
    };

    if(index_min != i)
    {
        A[i] , A[index_min]
          temp = A[i];
          A[i] = A[index_min];
          A[index_min] = temp;

    };

  

    
    return 0;
}